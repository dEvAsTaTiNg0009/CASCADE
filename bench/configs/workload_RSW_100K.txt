# CASCADE Benchmark — Workload Configuration
# Workload: RSW (Extension RSW: scan-heavy reporting)
# Scale: 100K (100,000 operations)
# YCSB status: Paper-defined extension (NOT standard YCSB)
#
# These parameters are the exact values used in the benchmark harness.
# See include/workload.h workloadRSW() for the C++ definition.

workload_name    = RSW
description      = Extension RSW: scan-heavy reporting
scale            = 100000
num_ops          = 100000
record_count     = 50000
key_space        = 1000000
zipfian_theta    = 0.99

# Operation mix (must sum to 1.0)
read_frac        = 0.25
update_frac      = 0.25
insert_frac      = 0.0
delete_frac      = 0.0
scan_frac        = 0.5
rmw_frac         = 0.0
scan_length      = 100

# Benchmark protocol
repeats          = 5
seed_base        = 42
seed_rule        = seed_base + run_id  (same per run_id across all 11 workloads)
load_seed        = 99                  (fixed across all runs)
direct_io_default = false

# Engine configuration (matches all rigorous_bench runs)
memtable_capacity     = 4096
max_levels            = 7
bloom_bits_per_key    = 14
bloom_max_bytes_MB    = 256
block_cache_MB        = 64
