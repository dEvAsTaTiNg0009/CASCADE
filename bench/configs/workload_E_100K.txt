# CASCADE Benchmark — Workload Configuration
# Workload: E (Standard YCSB-E: 95% short scans / 5% insert)
# Scale: 100K (100,000 operations)
# YCSB status: Standard YCSB workload
#
# These parameters are the exact values used in the benchmark harness.
# See include/workload.h workloadE() for the C++ definition.

workload_name    = E
description      = Standard YCSB-E: 95% short scans / 5% insert
scale            = 100000
num_ops          = 100000
record_count     = 50000
key_space        = 1000000
zipfian_theta    = 0.99

# Operation mix (must sum to 1.0)
read_frac        = 0.0
update_frac      = 0.0
insert_frac      = 0.05
delete_frac      = 0.0
scan_frac        = 0.95
rmw_frac         = 0.0
scan_length      = 100

# Benchmark protocol
repeats          = 5
seed_base        = 42
seed_rule        = seed_base + run_id  (same per run_id across all 11 workloads)
load_seed        = 99                  (fixed across all runs)
direct_io_default = false

# Engine configuration (matches all rigorous_bench runs)
memtable_capacity     = 4096
max_levels            = 7
bloom_bits_per_key    = 14
bloom_max_bytes_MB    = 256
block_cache_MB        = 64
